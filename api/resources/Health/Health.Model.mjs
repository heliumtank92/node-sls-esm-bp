const HealthModel = {
  get
}

export default HealthModel

async function get () {
  return new Promise((resolve, reject) => {
    // setTimeout(() => { resolve({ pan: 'abc' }) }, 50000)
    // reject(new Error('invalid'))
    // resolve({ pan: 'abc' })
  })
}
